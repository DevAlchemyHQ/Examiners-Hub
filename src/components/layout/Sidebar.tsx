export const Sidebar: React.FC<{ isLoading?: boolean }> = ({ isLoading }) => {
  return (
    <div className="lg:col-span-2 h-full">
      {/* Empty sidebar - image grid will take full left panel */}
    </div>
  );
};